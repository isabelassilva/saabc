
def msg_definer(character):
    character = character.upper();
    if (character == 'A'):
        dots = "Letter A, dot 1."
    if (character == 'B'):
        dots = "Letter B, dots 1 and 2."
    if (character == 'C'):
        dots = "Letter C, dots 1 and 4."
    if (character == 'D'):
        dots = "Letter D, dots 1, 4 and 5."
    if (character == 'E'):
        dots = "Letter E, dots 1 and 5."
    if (character == 'F'):
        dots = "Letter F, dots 1, 2 and 4."
    if (character == 'G'):
        dots = "Letter G, dots 1, 2, 4 and 5."
    if (character == 'H'):
        dots = "Letter H, dots 1, 2 and 5."
    if (character == 'I'):
        dots = "Letter I, dots 2 and 4."
    if (character == 'J'):
        dots = "Letter J, dots 2, 4 and 5"
    if (character == 'K'):
        dots = "Letter K, dots 1 and 3."
    if (character == 'L'):
        dots = "Letter L, dots 1, 2 and 3."
    if (character == 'M'):
        dots = "Letter M, dots 1, 3 and 4."
    if (character == 'N'):
        dots = "Letter N, dots 1, 3, 4 and 5."
    if (character == 'O'):
        dots = "Letter O, dots 1, 3 and 5."
    if (character == 'P'):
        dots = "Letter P, dots 1, 2, 3 and 4."
    if (character == 'Q'):
        dots = "Letter Q, dots 1, 2, 3, 4 and 5."
    if (character == 'R'):
        dots = "Letter R, dots 1, 2, 3 and 5"
    if (character == 'S'):
        dots = "Letter S, dots 2, 3 and 4."
    if (character == 'T'):
        dots = "Letter T, dots 2, 3, 4 and 5."
    if (character == 'U'):
        dots = "Letter U, dots 1, 3 and 6."
    if (character == 'V'):
        dots = "Letter V, dots 1, 2, 3 and 6."
    if (character == 'X'):
        dots = "Letter X, dots dots 1, 3, 4 and 6."
    if (character == 'Y'):
        dots = "Letter Y, dots 1, 3, 4, 5 and 6."
    if (character == 'Z'):
        dots = "Letter Z, dots 1, 3, 5 and 6."
    if (character == '1'):
        dots = "Number 1, dot 1."
    if (character == '2'):
        dots = "Number 2, dots 1 and 2."
    if (character == '3'):
        dots = "Number 3, dots 1 and 4."
    if (character == '4'):
        dots = "Number 4, dots 1, 4 and 5."
    if (character == '5'):
        dots = "Number 5, dots 1 and 5."
    if (character == '6'):
        dots = "Number 6, dots 1, 2, and 4."
    if (character == '7'):
        dots = "Number 7, dots 1, 2, 4 and 5."
    if (character == '8'):
        dots = "Number 8, dots 1, 2 and 5."
    if (character == '9'):
        dots = "Number 9, dots 2 and 4."
    if (character == '0'):
        dots = "Number 0, dots 2, 4 and 5."
    return dots
